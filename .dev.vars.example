# Copy to .dev.vars to restrict who may claim the pre-accounts legacy data
# (see docs/DEPLOY.md) on first registration. Leave unset for local dev —
# whoever registers first claims it, which is fine on a machine only you use.
# In production this is set with: wrangler secret put LEGACY_CLAIM_EMAIL
# LEGACY_CLAIM_EMAIL=you@example.com
